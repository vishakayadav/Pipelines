- group:
   description: "Essential development tools"
   checks:
     - version:
         cmd: node --version
         range: ^10.x.x
     - reachable:
         - deploy
         - deploy/production.git
         - deploy/production-www